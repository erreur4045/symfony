{% extends 'base.html.twig' %}

{% block title %}Connection!{% endblock %}

{% block body %}

    <div class="container">
        <div class="row">
            <div class="col-md-12">
                <h1 class="text-center">Login</h1>
            </div>
        </div>
        <div class="row justify-content-md-center text-center">
{#            <div class="col-md-4">
            {{ form_start(form, {'action': path('userconnexion'), 'method': 'POST'}) }}
            {{ form_row(form.mail,
            {'attr' :{'placeholder' : 'Votre mail', 'name' : '_username'}})
            }}
            {{ form_row(form.password,
            {'label' : 'Mot de passe', 'attr': {'placeholder' : 'Votre mot de passe', 'name' : '_password'}})
            }}
                <div class="text-center py-4">
                    <button type="submit" class="btn btn-primary btn-md">Login</button>
                </div>
            {{ form_end(form) }}
            <div class="text-center">
                <a href="#">Forgot password ?</a>
            </div>
            </div>#}
            {% if error %}
            <div class="alert alert-danger">{{ error.messageKey | trans(error.messagedata, 'security') }}</div>
            {% endif %}
            <div class="col-md-4">
            <form action="{{ path('userconnexion') }}" method="post">
                <div class="form-group">
                    <input type="text" placeholder="Votre mail" required name="_username" class="form-control" value="{{ last_username }}">
                </div>
                <div class="form-group">
                    <input type="password" placeholder="Votre mot de passe" required name="_password" class="form-control">
                </div>
                <div class="text-center py-4">
                    <button type="submit" class="btn btn-primary btn-md">Login</button>
                </div>
            </form>
    </div>
    </div>
    </div>
{% endblock %}
